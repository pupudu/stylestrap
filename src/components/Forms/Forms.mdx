---
name: Form
menu: Components
---

# Form Components

import { PropsTable, Preview, Playground } from '../docs';
import { FormInput, HelpText, FormSelect } from '../';

<form>
    <FormInput 
        type="number"
        label="Refund Amount"
        id="amount"
        placeholder="0.00"
        aria-describedby="currency"
        helpText="Refund amount cannot be more than the accounts total unapplied credit"
        error={''}
        touched={false}
    />
    <FormSelect></FormSelect>
</form>

<Props of={FormInput} />
