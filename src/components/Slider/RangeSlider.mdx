---
name: RangeSlider
menu: Components
---

import { PropsTable, Preview, Playground } from '../docs';
import { RangeSlider } from "../";

# Slider

<div id="abc">low: 10, high: 20</div>

<Playground>
  <RangeSlider min="0" max="90" value={[10, 20]} overlap onChange={([a, b]) => {
     document.getElementById("abc").innerText = `low: ${a}, high: ${b}`
  }}/>
</Playground>
